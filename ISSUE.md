# ISSUE Backlog: 機能改善リスト

## 目的
- 長時間利用でも安定して動作すること
- YouTube 側 UI 変更に強く、壊れにくいこと
- 使い勝手を上げ、誤操作やストレスを減らすこと

## 優先度 High

### 1. セレクタ互換性レイヤーを導入する
- 内容:
	- 主要 DOM 取得を 1 箇所に集約し、複数セレクタ候補を持てる構造にする
	- セレクタヒット率をログで計測し、壊れたときの検知を早める
- 完了条件:
	- 主要機能がセレクタ定義ファイル経由で要素取得する
	- セレクタ不一致時にフォールバックが動作する

### 2. localStorage 管理の健全化
- 内容:
	- 視聴済みデータに TTL または上限件数を設ける
	- 古いキーを自動クリーンアップする
	- 不正データ（undefined 文字列など）を起動時に補正する
- 完了条件:
	- データ肥大化で動作が劣化しない
	- 不正値が自動で除去される

### 3. 設定画面を追加する
- 内容:
	- ON/OFF 切替、強度設定、デバッグログ設定を UI から変更可能にする
	- 現在のバージョン表示と主要状態を設定画面で確認可能にする
- 完了条件:
	- ブラウザ拡張のオプション画面から設定が完結する
	- 手動で localStorage を触らなくても運用できる

## 優先度 Medium

### 4. 広告リロード通知の UX 改善
- 内容:
	- 通知を非侵襲化し、動画操作の邪魔を最小化する
	- キーボード操作（Enter / Esc）を明示し、短時間で消える表示モードを追加する
- 完了条件:
	- 通知表示中でも通常操作を妨げない
	- ユーザーが操作方法を即座に理解できる

### 5. バージョン表示トリガーのカスタム化
- 内容:
	- 現在のトリガー（隠しコマンド）に加えて、競合しにくい任意キー設定を選べるようにする
	- 誤発火抑制のためクールダウン時間を導入する
- 完了条件:
	- ユーザーがトリガー方法を選択できる
	- 視聴操作中の誤発火が低減する

### 6. エラー回復の強化
- 内容:
	- 例外発生時に対象機能だけを自動再初期化する
	- 連続エラー時は該当機能を一時停止して全体クラッシュを防ぐ
- 完了条件:
	- 単一機能の失敗で全機能が停止しない
	- 一時停止状態がログで確認できる

## 優先度 Low

### 7. 計測ログの見える化
- 内容:
	- flush 時間、保護モード遷移回数、対象要素件数を計測する
	- 必要時にコンソールへサマリを出せるコマンドを追加する
- 完了条件:
	- パフォーマンス悪化を数値で比較できる
	- 回帰時の原因追跡がしやすい

### 8. 自動テストの最小整備
- 内容:
	- URL 変化、広告表示、プレイリスト遷移の主要シナリオを自動テスト化する
	- CI で最低限の回帰チェックを回す
- 完了条件:
	- 主要フローの回帰を機械的に検出できる
	- リリース前確認の手動負荷を減らせる

### 9. データ同期基盤を導入する（chrome.storage.sync）
- 内容:
	- 視聴済みデータを localStorage から chrome.storage.sync へ段階移行する
	- 同期容量制限を考慮し、最近データのみ sync / 全量は local のハイブリッド運用を検討する
	- 初回起動時のマイグレーションと重複解決ルール（updatedAt 優先）を実装する
- 完了条件:
	- 同一 Google アカウントの別 PC で視聴済み状態が同期される
	- 同期失敗時でも既存の local データで継続動作できる
	- 既存ユーザーのデータが欠損せず移行できる

## 次に着手する候補
- 1. セレクタ互換性レイヤー
- 2. データ同期基盤（chrome.storage.sync）
- 3. localStorage 管理の健全化
