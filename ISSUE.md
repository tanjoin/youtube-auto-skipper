# ISSUE: YouTube「後で見る」長時間再生時の重さ・クラッシュ対策

## 背景
YouTube の「後で見る」を長時間再生していると、動作が重くなったり、タブがクラッシュすることがある。

## 想定される主因
1. DOM 全体の MutationObserver 監視が常時有効で、更新頻度が高いページで処理が連続実行される。
2. 1 回の監視イベントで複数コントローラが連鎖実行され、イベント発火も重なって処理密度が上がる。
3. /watch ページでもフィード向け処理が動作し、不要な監視・走査が発生する。

## 改善方針（実装順）

### 1. まず負荷上限を作る（即効・低リスク）
- MutationHub に最小実行間隔を導入する（目安: 300-500ms）。
- タブ非表示時はさらに間引く（目安: 2000ms）。
- 目的: 長時間再生時の CPU スパイクと熱暴走を抑える。

### 2. ページ種別で処理を分離する
- /watch では再生系に必要な処理のみ有効化する。
- フィード向け処理（トピック削除など）は /watch で停止または低頻度化する。
- 目的: 不要処理の常時実行を減らす。

### 3. イベント嵐を抑える
- movieCountChange などの発火をデバウンスする。
- 同一フレーム内の重複発火を統合する。
- 目的: 連鎖更新による無駄な再走査を減らす。

### 4. フェイルセーフを入れる
- pagehide / URL 変化時に監視解除や後始末を行う。
- flush 時間が閾値を超えたら重い機能を一時停止する保護モードを導入する。

## 検証基準
1. 2 時間連続再生で CPU 高負荷が持続しない。
2. 8 時間放置再生でタブクラッシュしない。
3. flush 平均時間と p95 を計測し、改善前後で比較できる。

## 優先実装案
- 直近は「1. 負荷上限」を先に実装する。
- その後「2. ページ種別分離」「3. イベント抑制」を適用する。
